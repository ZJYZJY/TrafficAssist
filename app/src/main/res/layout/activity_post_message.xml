<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.demo.call122.ReleaseActivity">

    <EditText
        android:id="@+id/accident_edit"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:background="@drawable/edittext_bg"
        android:gravity="left|top"
        android:hint="在此简要描述事故的概况"
        android:maxLength="140"
        android:paddingLeft="9dp"
        android:paddingRight="9dp"
        android:textSize="18dp" />

    <ImageView
        android:id="@+id/add_picture"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_below="@+id/accident_edit"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/add_pic_style" />

    <CheckBox
        android:id="@+id/isSerious"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@+id/add_picture"
        android:layout_marginTop="10dp"
        android:checked="false"
        android:paddingLeft="10dp"
        android:text="严重交通事故" />

    <Button
        android:id="@+id/btn_commit"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:textColor="#ffffff"
        android:textSize="20sp"
        android:background="@drawable/btn_commit"
        android:layout_marginTop="20dp"
        android:text="提交报案信息" />

</LinearLayout>
